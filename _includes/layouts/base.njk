<!doctype html>

<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    {% if title %}
    <title>{{ title }} - {{ site.title }}</title>
    {% else %}
    <title>{{ site.title }}</title>
    {% endif %}
    <meta name="description" content="{{ description or site.description }}">
    <link rel="stylesheet" href="/styles.css">
    <link rel="alternate" href="/feed.xml" type="application/atom+xml" title="{{ site.title }}">
    <link rel="alternate" href="/feed.json" type="application/json" title="{{ site.title }}">

    {% if isEnableGoogleAnalytics %}
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-B3YTF8DXDN"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-B3YTF8DXDN');
    </script>
    {% endif %}
  </head>
  <body>
    <nav class="navbar">
      <a href="/" class="navbar-home">
        <strong>{{ site.title }}</strong>
      </a>

      <ul class="navbar-links">
      {%- for data in search.pages("menu.visible=true", "menu.order") %}
        <li>
          <a href="{{ data.url }}"{% if data.url == url %} aria-current="page"{% endif %}>
            {{ data.menu.title or data.title }}
          </a>
        </li>
      {%- endfor %}
        <li>
          <a href="https://github.com/jyuch">
            GitHub
          </a>
        </li>
        <li>
          <a href="https://www.jyuch.dev/">
            Blog
          </a>
        </li>
        <li>
          <a href="https://speakerdeck.com/jyuch">
            Speaker Deck
          </a>
        </li>
      </ul>

      <div class="navbar-search">
        <div id="search"></div>
      </div>
    </nav>

    <main class="{{ bodyClass }}">
      {{ content | safe }}
    </main>

    <!-- Current page: {{ url }} -->
  </body>
</html>
